<div class="flex items-center gap-2">
    <div class="flex flex-col">
        <span class="font-semibold text-gray-900">{{ $item->name }}</span>
        <span class="text-sm text-gray-500">{{ $item->domain_name }}.{{ $item->tld }}</span>
    </div>
</div>
