@props([
    'title' => '',
    'subtitle' => null,
    'backRoute' => null,
    'backLabel' => 'Back',
    'actions' => null
])

<div class="space-y-6">
    <!-- Page Header -->
    <div class="flex max-md:flex-col items-start gap-4">
        <div class="min-w-0 flex-1">
            @if($backRoute)
                <flux:button variant="ghost" size="sm" href="{{ $backRoute }}" icon="arrow-left" class="mb-2">
                    {{ $backLabel }}
                </flux:button>
            @endif
            
            <flux:heading size="xl" level="1">{{ $title }}</flux:heading>
            @if($subtitle)
                <flux:text class="mt-2 text-base text-zinc-600 dark:text-zinc-400">{{ $subtitle }}</flux:text>
            @endif
        </div>
        
        @if($actions)
            <div class="flex-shrink-0">
                {{ $actions }}
            </div>
        @endif
    </div>

    <flux:separator variant="subtle" />

    <!-- Main Content Grid -->
    <div class="flex max-md:flex-col items-start gap-6">
        <!-- Left Column (Main Content - 2/3 width) -->
        <div class="w-full md:w-2/3 space-y-6">
            {{ $slot }}
        </div>

        <!-- Right Column (Sidebar - 1/3 width) -->
        <div class="w-full md:w-1/3 space-y-6">
            {{ $sidebar ?? '' }}
        </div>
    </div>
</div>