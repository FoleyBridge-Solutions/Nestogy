<div class="h-full">
    <flux:card class="h-full">
        <div class="p-6 border-b border-zinc-200 dark:border-zinc-700">
            <flux:heading size="lg" class="flex items-center gap-2">
                <flux:icon.credit-card class="size-5 text-green-500" />
                Payment Tracking
            </flux:heading>
        </div>
        
        <div class="p-6">
            @if($loading)
                <div class="flex items-center justify-center h-64">
                    <flux:icon.arrow-path class="size-8 animate-spin text-zinc-400" />
                </div>
            @else
                @if(empty($data['items']))
                    <div class="flex flex-col items-center justify-center h-64 text-center">
                        <flux:icon.credit-card class="size-16 text-zinc-300 mb-4" />
                        <flux:heading size="lg" class="text-zinc-500 mb-2">No Payment Data</flux:heading>
                        <flux:text class="text-zinc-400">Payment tracking data will appear here once configured.</flux:text>
                    </div>
                @else
                    <!-- Payment Stats -->
                    <div class="grid grid-cols-1 md:grid-cols-3 gap-4 mb-6">
                        @foreach($data['stats'] as $stat)
                            <div class="bg-zinc-50 dark:bg-zinc-800 rounded-lg p-4 border border-zinc-200 dark:border-zinc-700">
                                <div class="flex items-start justify-between">
                                    <div class="flex-1">
                                        <flux:text class="text-xs text-zinc-500 dark:text-zinc-400">
                                            {{ $stat['label'] }}
                                        </flux:text>
                                        
                                        <div class="mt-1">
                                            <flux:heading size="lg" class="font-semibold">
                                                @if($stat['type'] === 'currency')
                                                    ${{ number_format($stat['value'], 2) }}
                                                @else
                                                    {{ number_format($stat['value']) }}
                                                @endif
                                            </flux:heading>
                                        </div>
                                    </div>
                                    
                                    <div class="ml-3">
                                        @switch($stat['icon'])
                                            @case('currency-dollar')
                                                <flux:icon.currency-dollar class="size-5 text-green-500" />
                                                @break
                                            @case('clock')
                                                <flux:icon.clock class="size-5 text-blue-500" />
                                                @break
                                            @case('check-circle')
                                                <flux:icon.check-circle class="size-5 text-green-500" />
                                                @break
                                            @default
                                                <flux:icon.credit-card class="size-5 text-zinc-400" />
                                        @endswitch
                                    </div>
                                </div>
                            </div>
                        @endforeach
                    </div>
                    
                    <!-- Payment Items -->
                    <div class="space-y-3">
                        @foreach($data['items'] as $item)
                            <div class="bg-white dark:bg-zinc-800 border border-zinc-200 dark:border-zinc-700 rounded-lg p-4">
                                <div class="flex items-center justify-between">
                                    <div class="flex-1">
                                        <div class="flex items-center gap-2">
                                            <flux:text class="font-medium">{{ $item['description'] }}</flux:text>
                                            @if($item['status'] === 'completed')
                                                <flux:badge color="green" size="sm">Paid</flux:badge>
                                            @elseif($item['status'] === 'pending')
                                                <flux:badge color="yellow" size="sm">Pending</flux:badge>
                                            @elseif($item['status'] === 'failed')
                                                <flux:badge color="red" size="sm">Failed</flux:badge>
                                            @endif
                                        </div>
                                        
                                        <div class="mt-1 flex items-center gap-4">
                                            <flux:text class="text-sm text-zinc-500">
                                                {{ $item['date'] }}
                                            </flux:text>
                                            @if($item['method'])
                                                <flux:text class="text-sm text-zinc-500">
                                                    {{ $item['method'] }}
                                                </flux:text>
                                            @endif
                                        </div>
                                    </div>
                                    
                                    <div class="text-right">
                                        <flux:text class="font-semibold">
                                            ${{ number_format($item['amount'], 2) }}
                                        </flux:text>
                                        
                                        @if($item['client'])
                                            <div class="mt-1">
                                                <flux:text class="text-sm text-zinc-500">
                                                    {{ $item['client'] }}
                                                </flux:text>
                                            </div>
                                        @endif
                                    </div>
                                </div>
                            </div>
                        @endforeach
                    </div>
                @endif
                
                <!-- Summary -->
                <div class="mt-6 pt-4 border-t border-zinc-200 dark:border-zinc-700">
                    <div class="grid grid-cols-2 lg:grid-cols-4 gap-4">
                        <div>
                            <flux:text class="text-xs text-zinc-500">Period</flux:text>
                            <flux:text class="font-medium">{{ now()->format('F Y') }}</flux:text>
                        </div>
                        <div>
                            <flux:text class="text-xs text-zinc-500">Last Updated</flux:text>
                            <flux:text class="font-medium">{{ now()->format('g:i A') }}</flux:text>
                        </div>
                        <div>
                            <flux:text class="text-xs text-zinc-500">Currency</flux:text>
                            <flux:text class="font-medium">USD</flux:text>
                        </div>
                        <div>
                            <flux:text class="text-xs text-zinc-500">Status</flux:text>
                            <div class="flex items-center gap-1">
                                <flux:icon.check-circle class="size-4 text-green-500" />
                                <flux:text class="font-medium text-green-600">Active</flux:text>
                            </div>
                        </div>
                    </div>
                </div>
            @endif
        </div>
    </flux:card>
</div>