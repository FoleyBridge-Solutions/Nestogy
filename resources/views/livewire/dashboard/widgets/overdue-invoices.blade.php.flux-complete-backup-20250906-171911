<div class="h-full">
    <flux:card class="h-full">
        <div class="p-6 border-b border-zinc-200 dark:border-zinc-700">
            <flux:heading size="lg" class="flex items-center gap-2">
                <flux:icon.exclamation-triangle class="size-5 text-red-500" />
                Overdue Invoices
            </flux:heading>
        </div>
        
        <div class="p-6">
            @if($loading)
                <div class="flex items-center justify-center h-64">
                    <flux:icon.arrow-path class="size-8 animate-spin text-zinc-400" />
                </div>
            @else
                @if(empty($data['items']))
                    <div class="flex flex-col items-center justify-center h-64 text-center">
                        <flux:icon.check-circle class="size-16 text-green-300 mb-4" />
                        <flux:heading size="lg" class="text-green-600 mb-2">No Overdue Invoices</flux:heading>
                        <flux:text class="text-zinc-400">Great! All invoices are current.</flux:text>
                    </div>
                @else
                    <!-- Overdue Stats -->
                    <div class="grid grid-cols-1 md:grid-cols-3 gap-4 mb-6">
                        @foreach($data['stats'] as $stat)
                            <div class="bg-zinc-50 dark:bg-zinc-800 rounded-lg p-4 border border-zinc-200 dark:border-zinc-700">
                                <div class="flex items-start justify-between">
                                    <div class="flex-1">
                                        <flux:text class="text-xs text-zinc-500 dark:text-zinc-400">
                                            {{ $stat['label'] }}
                                        </flux:text>
                                        
                                        <div class="mt-1">
                                            <flux:heading size="lg" class="font-semibold text-red-600">
                                                @if($stat['type'] === 'currency')
                                                    ${{ number_format($stat['value'], 2) }}
                                                @else
                                                    {{ number_format($stat['value']) }}
                                                @endif
                                            </flux:heading>
                                        </div>
                                    </div>
                                    
                                    <div class="ml-3">
                                        @switch($stat['icon'] ?? 'exclamation-triangle')
                                            @case('exclamation-triangle')
                                                <flux:icon.exclamation-triangle class="size-5 text-red-500" />
                                                @break
                                            @case('currency-dollar')
                                                <flux:icon.currency-dollar class="size-5 text-red-500" />
                                                @break
                                            @case('document-text')
                                                <flux:icon.document-text class="size-5 text-red-500" />
                                                @break
                                            @default
                                                <flux:icon.exclamation-triangle class="size-5 text-red-500" />
                                        @endswitch
                                    </div>
                                </div>
                            </div>
                        @endforeach
                    </div>
                    
                    <!-- Overdue Invoice List -->
                    <div class="space-y-3">
                        @foreach($data['items'] as $item)
                            <div class="bg-white dark:bg-zinc-800 border border-red-200 dark:border-red-800 rounded-lg p-4">
                                <div class="flex items-center justify-between">
                                    <div class="flex-1">
                                        <div class="flex items-center gap-2">
                                            <flux:text class="font-medium">{{ $item['client_name'] }}</flux:text>
                                            <flux:text class="text-sm text-zinc-500">
                                                #{{ $item['invoice_number'] }}
                                            </flux:text>
                                            
                                            @if($item['days_overdue'] > 90)
                                                <flux:badge color="red" size="sm">{{ $item['days_overdue'] }}+ days</flux:badge>
                                            @elseif($item['days_overdue'] > 60)
                                                <flux:badge color="orange" size="sm">{{ $item['days_overdue'] }} days</flux:badge>
                                            @elseif($item['days_overdue'] > 30)
                                                <flux:badge color="yellow" size="sm">{{ $item['days_overdue'] }} days</flux:badge>
                                            @else
                                                <flux:badge color="red" size="sm">{{ $item['days_overdue'] }} days</flux:badge>
                                            @endif
                                        </div>
                                        
                                        <div class="mt-1 flex items-center gap-4">
                                            <flux:text class="text-sm text-zinc-500">
                                                Due: {{ $item['due_date'] }}
                                            </flux:text>
                                            @if($item['last_contact'])
                                                <flux:text class="text-sm text-zinc-500">
                                                    Last contact: {{ $item['last_contact'] }}
                                                </flux:text>
                                            @endif
                                        </div>
                                    </div>
                                    
                                    <div class="text-right">
                                        <flux:text class="font-semibold text-red-600">
                                            ${{ number_format($item['amount'], 2) }}
                                        </flux:text>
                                        
                                        @if($item['payment_method'])
                                            <div class="mt-1">
                                                <flux:text class="text-sm text-zinc-500">
                                                    {{ $item['payment_method'] }}
                                                </flux:text>
                                            </div>
                                        @endif
                                    </div>
                                </div>
                            </div>
                        @endforeach
                    </div>
                @endif
                
                <!-- Summary -->
                <div class="mt-6 pt-4 border-t border-zinc-200 dark:border-zinc-700">
                    <div class="grid grid-cols-2 lg:grid-cols-4 gap-4">
                        <div>
                            <flux:text class="text-xs text-zinc-500">Total Overdue</flux:text>
                            <flux:text class="font-medium text-red-600">$0.00</flux:text>
                        </div>
                        <div>
                            <flux:text class="text-xs text-zinc-500">Count</flux:text>
                            <flux:text class="font-medium">0</flux:text>
                        </div>
                        <div>
                            <flux:text class="text-xs text-zinc-500">Avg Days Overdue</flux:text>
                            <flux:text class="font-medium">0</flux:text>
                        </div>
                        <div>
                            <flux:text class="text-xs text-zinc-500">Last Updated</flux:text>
                            <flux:text class="font-medium">{{ now()->format('g:i A') }}</flux:text>
                        </div>
                    </div>
                </div>
            @endif
        </div>
    </flux:card>
</div>