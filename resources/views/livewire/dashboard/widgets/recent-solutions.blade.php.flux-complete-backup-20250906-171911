<div class="h-full">
    <flux:card class="h-full">
        <div class="p-6 border-b border-zinc-200 dark:border-zinc-700">
            <flux:heading size="lg" class="flex items-center gap-2">
                <flux:icon.light-bulb class="size-5 text-yellow-500" />
                Recent Solutions
            </flux:heading>
        </div>
        
        <div class="p-6">
            @if($loading)
                <div class="flex items-center justify-center h-64">
                    <flux:icon.arrow-path class="size-8 animate-spin text-zinc-400" />
                </div>
            @else
                @if(empty($data['items']))
                    <div class="flex flex-col items-center justify-center h-64 text-center">
                        <flux:icon.light-bulb class="size-16 text-zinc-300 mb-4" />
                        <flux:heading size="lg" class="text-zinc-500 mb-2">No Recent Solutions</flux:heading>
                        <flux:text class="text-zinc-400">Recently resolved tickets and solutions will appear here.</flux:text>
                    </div>
                @else
                    <!-- Solution Stats -->
                    <div class="grid grid-cols-1 md:grid-cols-3 gap-4 mb-6">
                        @foreach($data['stats'] as $stat)
                            <div class="bg-zinc-50 dark:bg-zinc-800 rounded-lg p-4 border border-zinc-200 dark:border-zinc-700">
                                <div class="flex items-start justify-between">
                                    <div class="flex-1">
                                        <flux:text class="text-xs text-zinc-500 dark:text-zinc-400">
                                            {{ $stat['label'] }}
                                        </flux:text>
                                        
                                        <div class="mt-1">
                                            <flux:heading size="lg" class="font-semibold">
                                                @if($stat['type'] === 'time')
                                                    {{ number_format($stat['value'], 1) }}h
                                                @else
                                                    {{ number_format($stat['value']) }}
                                                @endif
                                            </flux:heading>
                                        </div>
                                        
                                        @if(isset($stat['trend']))
                                            <div class="mt-1 text-xs">
                                                @if($stat['trend'] > 0)
                                                    <span class="text-green-600">+{{ $stat['trend'] }}</span>
                                                @elseif($stat['trend'] < 0)
                                                    <span class="text-red-600">{{ $stat['trend'] }}</span>
                                                @else
                                                    <span class="text-zinc-500">No change</span>
                                                @endif
                                                <span class="text-zinc-500"> from last week</span>
                                            </div>
                                        @endif
                                    </div>
                                    
                                    <div class="ml-3">
                                        @switch($stat['icon'] ?? 'light-bulb')
                                            @case('light-bulb')
                                                <flux:icon.light-bulb class="size-5 text-yellow-500" />
                                                @break
                                            @case('clock')
                                                <flux:icon.clock class="size-5 text-blue-500" />
                                                @break
                                            @case('check-circle')
                                                <flux:icon.check-circle class="size-5 text-green-500" />
                                                @break
                                            @default
                                                <flux:icon.light-bulb class="size-5 text-zinc-400" />
                                        @endswitch
                                    </div>
                                </div>
                            </div>
                        @endforeach
                    </div>
                    
                    <!-- Recent Solutions List -->
                    <div class="space-y-3">
                        @foreach($data['items'] as $item)
                            <div class="bg-white dark:bg-zinc-800 border border-zinc-200 dark:border-zinc-700 rounded-lg p-4 hover:border-green-300 dark:hover:border-green-600 transition-colors">
                                <div class="flex items-start justify-between">
                                    <div class="flex-1">
                                        <div class="flex items-center gap-2">
                                            <flux:text class="font-medium">{{ $item['title'] }}</flux:text>
                                            
                                            @if($item['priority'])
                                                @if($item['priority'] === 'Critical')
                                                    <flux:badge color="red" size="sm">Critical</flux:badge>
                                                @elseif($item['priority'] === 'High')
                                                    <flux:badge color="orange" size="sm">High</flux:badge>
                                                @elseif($item['priority'] === 'Medium')
                                                    <flux:badge color="yellow" size="sm">Medium</flux:badge>
                                                @else
                                                    <flux:badge color="gray" size="sm">Low</flux:badge>
                                                @endif
                                            @endif
                                            
                                            <flux:badge color="green" size="sm">Resolved</flux:badge>
                                        </div>
                                        
                                        @if($item['solution'])
                                            <flux:text class="text-sm text-zinc-600 dark:text-zinc-400 mt-2">
                                                {{ Str::limit($item['solution'], 200) }}
                                            </flux:text>
                                        @endif
                                        
                                        <div class="mt-2 flex items-center gap-4">
                                            <flux:text class="text-sm text-zinc-500">
                                                Ticket #{{ $item['ticket_number'] }}
                                            </flux:text>
                                            <flux:text class="text-sm text-zinc-500">
                                                {{ $item['client_name'] }}
                                            </flux:text>
                                            <flux:text class="text-sm text-zinc-500">
                                                Resolved by {{ $item['resolved_by'] }}
                                            </flux:text>
                                        </div>
                                        
                                        <div class="mt-1 flex items-center gap-4">
                                            <flux:text class="text-sm text-zinc-500">
                                                Resolution time: {{ $item['resolution_time'] }}
                                            </flux:text>
                                            <flux:text class="text-sm text-zinc-500">
                                                {{ $item['resolved_at'] }}
                                            </flux:text>
                                        </div>
                                    </div>
                                    
                                    <div class="ml-4 text-right">
                                        <div class="flex items-center gap-1">
                                            <flux:icon.check-circle class="size-5 text-green-500" />
                                        </div>
                                        
                                        @if($item['category'])
                                            <flux:text class="text-xs text-zinc-500 mt-1">
                                                {{ $item['category'] }}
                                            </flux:text>
                                        @endif
                                    </div>
                                </div>
                            </div>
                        @endforeach
                    </div>
                @endif
                
                <!-- Summary -->
                <div class="mt-6 pt-4 border-t border-zinc-200 dark:border-zinc-700">
                    <div class="grid grid-cols-2 lg:grid-cols-4 gap-4">
                        <div>
                            <flux:text class="text-xs text-zinc-500">Resolved Today</flux:text>
                            <flux:text class="font-medium">0</flux:text>
                        </div>
                        <div>
                            <flux:text class="text-xs text-zinc-500">Avg Resolution Time</flux:text>
                            <flux:text class="font-medium">0h</flux:text>
                        </div>
                        <div>
                            <flux:text class="text-xs text-zinc-500">Resolution Rate</flux:text>
                            <flux:text class="font-medium">0%</flux:text>
                        </div>
                        <div>
                            <flux:text class="text-xs text-zinc-500">Last Updated</flux:text>
                            <flux:text class="font-medium">{{ now()->format('g:i A') }}</flux:text>
                        </div>
                    </div>
                </div>
            @endif
        </div>
    </flux:card>
</div>