<div class="h-full">
    <flux:card class="h-full">
        <div class="p-6 border-b border-zinc-200 dark:border-zinc-700">
            <flux:heading size="lg" class="flex items-center gap-2">
                <flux:icon.currency-dollar class="size-5 text-green-500" />
                Collection Metrics
            </flux:heading>
        </div>
        
        <div class="p-6">
            @if($loading)
                <div class="flex items-center justify-center h-64">
                    <flux:icon.arrow-path class="size-8 animate-spin text-zinc-400" />
                </div>
            @else
                @if(empty($data['items']))
                    <div class="flex flex-col items-center justify-center h-64 text-center">
                        <flux:icon.currency-dollar class="size-16 text-zinc-300 mb-4" />
                        <flux:heading size="lg" class="text-zinc-500 mb-2">No Collection Data</flux:heading>
                        <flux:text class="text-zinc-400">Collection metrics will appear here once data is available.</flux:text>
                    </div>
                @else
                    <!-- Collection Stats -->
                    <div class="grid grid-cols-1 md:grid-cols-4 gap-4 mb-6">
                        @foreach($data['stats'] as $stat)
                            <div class="bg-zinc-50 dark:bg-zinc-800 rounded-lg p-4 border border-zinc-200 dark:border-zinc-700">
                                <div class="flex items-start justify-between">
                                    <div class="flex-1">
                                        <flux:text class="text-xs text-zinc-500 dark:text-zinc-400">
                                            {{ $stat['label'] }}
                                        </flux:text>
                                        
                                        <div class="mt-1">
                                            <flux:heading size="lg" class="font-semibold">
                                                @if($stat['type'] === 'currency')
                                                    ${{ number_format($stat['value'], 2) }}
                                                @elseif($stat['type'] === 'percentage')
                                                    {{ number_format($stat['value'], 1) }}%
                                                @else
                                                    {{ number_format($stat['value']) }}
                                                @endif
                                            </flux:heading>
                                        </div>
                                        
                                        @if(isset($stat['trend']))
                                            <div class="mt-1 text-xs">
                                                @if($stat['trend'] > 0)
                                                    <span class="text-green-600">+{{ $stat['trend'] }}%</span>
                                                @elseif($stat['trend'] < 0)
                                                    <span class="text-red-600">{{ $stat['trend'] }}%</span>
                                                @else
                                                    <span class="text-zinc-500">No change</span>
                                                @endif
                                            </div>
                                        @endif
                                    </div>
                                    
                                    <div class="ml-3">
                                        @switch($stat['icon'] ?? 'currency-dollar')
                                            @case('currency-dollar')
                                                <flux:icon.currency-dollar class="size-5 text-green-500" />
                                                @break
                                            @case('clock')
                                                <flux:icon.clock class="size-5 text-blue-500" />
                                                @break
                                            @case('chart-bar')
                                                <flux:icon.chart-bar class="size-5 text-purple-500" />
                                                @break
                                            @case('percentage')
                                                <flux:icon.chart-pie class="size-5 text-orange-500" />
                                                @break
                                            @default
                                                <flux:icon.currency-dollar class="size-5 text-zinc-400" />
                                        @endswitch
                                    </div>
                                </div>
                            </div>
                        @endforeach
                    </div>
                    
                    <!-- Collection Items -->
                    <div class="space-y-3">
                        @foreach($data['items'] as $item)
                            <div class="bg-white dark:bg-zinc-800 border border-zinc-200 dark:border-zinc-700 rounded-lg p-4">
                                <div class="flex items-center justify-between">
                                    <div class="flex-1">
                                        <div class="flex items-center gap-2">
                                            <flux:text class="font-medium">{{ $item['client_name'] }}</flux:text>
                                            @if($item['priority'] === 'high')
                                                <flux:badge color="red" size="sm">High Priority</flux:badge>
                                            @elseif($item['priority'] === 'medium')
                                                <flux:badge color="yellow" size="sm">Medium</flux:badge>
                                            @endif
                                        </div>
                                        
                                        <div class="mt-1 flex items-center gap-4">
                                            <flux:text class="text-sm text-zinc-500">
                                                {{ $item['days_overdue'] }} days overdue
                                            </flux:text>
                                            <flux:text class="text-sm text-zinc-500">
                                                Invoice #{{ $item['invoice_number'] }}
                                            </flux:text>
                                        </div>
                                    </div>
                                    
                                    <div class="text-right">
                                        <flux:text class="font-semibold text-red-600">
                                            ${{ number_format($item['amount'], 2) }}
                                        </flux:text>
                                        
                                        <div class="mt-1">
                                            <flux:text class="text-sm text-zinc-500">
                                                Due: {{ $item['due_date'] }}
                                            </flux:text>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        @endforeach
                    </div>
                @endif
                
                <!-- Summary -->
                <div class="mt-6 pt-4 border-t border-zinc-200 dark:border-zinc-700">
                    <div class="grid grid-cols-2 lg:grid-cols-4 gap-4">
                        <div>
                            <flux:text class="text-xs text-zinc-500">Total Outstanding</flux:text>
                            <flux:text class="font-medium">$0.00</flux:text>
                        </div>
                        <div>
                            <flux:text class="text-xs text-zinc-500">Collection Rate</flux:text>
                            <flux:text class="font-medium">0%</flux:text>
                        </div>
                        <div>
                            <flux:text class="text-xs text-zinc-500">Last Updated</flux:text>
                            <flux:text class="font-medium">{{ now()->format('g:i A') }}</flux:text>
                        </div>
                        <div>
                            <flux:text class="text-xs text-zinc-500">Currency</flux:text>
                            <flux:text class="font-medium">USD</flux:text>
                        </div>
                    </div>
                </div>
            @endif
        </div>
    </flux:card>
</div>