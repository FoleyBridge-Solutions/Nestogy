<div class="space-y-6">
    <!-- Header -->
    <div class="flex items-center justify-between">
        <div>
            <h3 class="text-lg font-medium text-gray-900 dark:text-white">Quote Templates</h3>
            <p class="text-sm text-gray-600 dark:text-gray-400">
                Choose from existing templates to speed up quote creation
            </p>
        </div>
        @if($this->hasSelected)
            <flux:button 
                wire:click="clearSelection"
                variant="ghost"
                size="sm"
            >
                Clear Selection
            </flux:button>
        @endif
    </div>

    <!-- Selected Template Display -->
    @if($this->hasSelected)
        <flux:card class="p-4 bg-primary-50 dark:bg-primary-900/20 border border-primary-200 dark:border-primary-800">
            <div class="flex items-start justify-between">
                <div class="flex-1">
                    <div class="flex items-center space-x-2">
                        <svg class="w-5 h-5 text-blue-600 dark:text-blue-400-600 dark:text-blue-600 dark:text-blue-400-400" fill="currentColor" viewBox="0 0 20 20">
                            <path fill-rule="evenodd" d="M16.707 5.293a1 1 0 010 1.414l-8 8a1 1 0 01-1.414 0l-4-4a1 1 0 011.414-1.414L8 12.586l7.293-7.293a1 1 0 011.414 0z" clip-rule="evenodd"/>
                        </svg>
                        <h4 class="font-medium text-blue-600 dark:text-blue-400-900 dark:text-blue-600 dark:text-blue-400-100">
                            {{ $selectedTemplate['name'] }}
                        </h4>
                    </div>
                    @if($selectedTemplate['description'])
                        <p class="text-sm text-blue-600 dark:text-blue-400-700 dark:text-blue-600 dark:text-blue-400-300 mt-1">
                            {{ $selectedTemplate['description'] }}
                        </p>
                    @endif
                    <div class="flex items-center space-x-4 mt-2 text-xs text-blue-600 dark:text-blue-400-600 dark:text-blue-600 dark:text-blue-400-400">
                        <span>{{ $selectedTemplate['category'] }}</span>
                        <span>{{ $selectedTemplate['items_count'] }} items</span>
                        <span>Used {{ $selectedTemplate['usage_count'] }} times</span>
                    </div>
                </div>
                <flux:button 
                    wire:click="showPreview({{ $selectedTemplate['id'] }})"
                    variant="ghost"
                    size="sm"
                    class="text-blue-600 dark:text-blue-400-600 hover:text-blue-600 dark:text-blue-400-800"
                >
                    Preview
                </flux:button>
            </div>
        </flux:card>
    @endif

    <!-- Search and Filters -->
    <div class="grid grid-cols-1 md:grid-cols-3 gap-4">
        <!-- Search -->
        <div class="md:col-span-2">
            <flux:input 
                wire:model.live="searchQuery"
                placeholder="Search templates by name, description, or category..."
                class="w-full"
            >
                <x-slot name="iconTrailing">
                    <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z"/>
                    </svg>
                </x-slot>
            </flux:input>
        </div>

        <!-- Category Filter -->
        <div>
            <flux:select 
                wire:model.live="selectedCategory"
                placeholder="All Categories"
            >
                <flux:option value="">All Categories</flux:option>
                @foreach($categories as $categoryId => $categoryName)
                    <flux:option value="{{ $categoryId }}">{{ $categoryName }}</flux:option>
                @endforeach
            </flux:select>
        </div>
    </div>

    <!-- Active Filters -->
    @if(!empty($searchQuery) || !empty($selectedCategory))
        <div class="flex items-center space-x-2">
            <span class="text-sm text-gray-600 dark:text-gray-400">Active filters:</span>
            @if(!empty($searchQuery))
                <span class="inline-flex items-center px-2 py-1 text-xs font-medium rounded-md bg-zinc-100 text-zinc-800 dark:bg-zinc-800 dark:text-zinc-200">
                    Search: "{{ $searchQuery }}"
                </span>
            @endif
            @if(!empty($selectedCategory))
                <span class="inline-flex items-center px-2 py-1 text-xs font-medium rounded-md bg-zinc-100 text-zinc-800 dark:bg-zinc-800 dark:text-zinc-200">
                    Category: {{ $categories[$selectedCategory] }}
                </span>
            @endif
            <flux:button 
                wire:click="clearFilters"
                variant="ghost"
                size="sm"
                class="text-sm"
            >
                Clear All
            </flux:button>
        </div>
    @endif

    <!-- Quick Access Sections -->
    @if(empty($searchQuery) && empty($selectedCategory))
        <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
            <!-- Popular Templates -->
            @if(!empty($this->popularTemplates))
                <div>
                    <h4 class="font-medium text-gray-900 dark:text-white mb-3">Most Popular</h4>
                    <div class="space-y-2">
                        @foreach($this->popularTemplates as $template)
                            <div class="p-3 border border-gray-200 dark:border-gray-700 rounded-lg hover:bg-gray-50 dark:hover:bg-gray-800 cursor-pointer"
                                 wire:click="selectTemplate({{ $template['id'] }})">
                                <div class="flex items-center justify-between">
                                    <div>
                                        <h5 class="font-medium text-gray-900 dark:text-white text-sm">
                                            {{ $template['name'] }}
                                        </h5>
                                        <p class="text-xs text-gray-500 dark:text-gray-400">
                                            {{ $template['category'] }} • {{ $template['items_count'] }} items
                                        </p>
                                    </div>
                                    <div class="text-xs text-gray-400">
                                        {{ $template['usage_count'] }} uses
                                    </div>
                                </div>
                            </div>
                        @endforeach
                    </div>
                </div>
            @endif

            <!-- Recent Templates -->
            @if(!empty($this->recentTemplates))
                <div>
                    <h4 class="font-medium text-gray-900 dark:text-white mb-3">Recently Updated</h4>
                    <div class="space-y-2">
                        @foreach($this->recentTemplates as $template)
                            <div class="p-3 border border-gray-200 dark:border-gray-700 rounded-lg hover:bg-gray-50 dark:hover:bg-gray-800 cursor-pointer"
                                 wire:click="selectTemplate({{ $template['id'] }})">
                                <div class="flex items-center justify-between">
                                    <div>
                                        <h5 class="font-medium text-gray-900 dark:text-white text-sm">
                                            {{ $template['name'] }}
                                        </h5>
                                        <p class="text-xs text-gray-500 dark:text-gray-400">
                                            {{ $template['category'] }} • {{ $template['items_count'] }} items
                                        </p>
                                    </div>
                                    <div class="text-xs text-gray-400">
                                        {{ \Carbon\Carbon::parse($template['updated_at'])->diffForHumans() }}
                                    </div>
                                </div>
                            </div>
                        @endforeach
                    </div>
                </div>
            @endif
        </div>
    @endif

    <!-- All Templates -->
    @if($this->hasTemplates)
        <div class="space-y-6">
            @if(!empty($searchQuery) || !empty($selectedCategory))
                <h4 class="font-medium text-gray-900 dark:text-white">
                    Search Results ({{ count($filteredTemplates) }} found)
                </h4>
            @else
                <h4 class="font-medium text-gray-900 dark:text-white">All Templates</h4>
            @endif

            <!-- Templates by Category -->
            @foreach($this->categorizedTemplates as $categoryGroup)
                <div>
                    <h5 class="text-md font-medium text-gray-800 dark:text-gray-200 mb-3">
                        {{ $categoryGroup['name'] }} ({{ $categoryGroup['count'] }})
                    </h5>
                    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4">
                        @foreach($categoryGroup['templates'] as $template)
                            <flux:card 
                                class="p-4 cursor-pointer hover:shadow-md transition-shadow {{ $selectedTemplate && $selectedTemplate['id'] === $template['id'] ? 'ring-2 ring-primary-500 bg-primary-50 dark:bg-primary-900/20' : '' }}"
                                wire:click="selectTemplate({{ $template['id'] }})"
                            >
                                <div class="space-y-3">
                                    <!-- Template Header -->
                                    <div class="flex items-start justify-between">
                                        <h6 class="font-medium text-gray-900 dark:text-white">
                                            {{ $template['name'] }}
                                        </h6>
                                        <div class="flex items-center space-x-1">
                                            @if($selectedTemplate && $selectedTemplate['id'] === $template['id'])
                                                <svg class="w-4 h-4 text-blue-600 dark:text-blue-400-600" fill="currentColor" viewBox="0 0 20 20">
                                                    <path fill-rule="evenodd" d="M16.707 5.293a1 1 0 010 1.414l-8 8a1 1 0 01-1.414 0l-4-4a1 1 0 011.414-1.414L8 12.586l7.293-7.293a1 1 0 011.414 0z" clip-rule="evenodd"/>
                                                </svg>
                                            @endif
                                            <flux:button 
                                                wire:click.stop="showPreview({{ $template['id'] }})"
                                                variant="ghost"
                                                size="sm"
                                                class="p-1"
                                            >
                                                <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 12a3 3 0 11-6 0 3 3 0 016 0z"/>
                                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M2.458 12C3.732 7.943 7.523 5 12 5c4.478 0 8.268 2.943 9.542 7-1.274 4.057-5.064 7-9.542 7-4.477 0-8.268-2.943-9.542-7z"/>
                                                </svg>
                                            </flux:button>
                                        </div>
                                    </div>

                                    <!-- Description -->
                                    @if($template['description'])
                                        <p class="text-sm text-gray-600 dark:text-gray-400 line-clamp-2">
                                            {{ $template['description'] }}
                                        </p>
                                    @endif

                                    <!-- Template Stats -->
                                    <div class="flex items-center justify-between text-xs text-gray-500 dark:text-gray-400">
                                        <div class="flex items-center space-x-3">
                                            <span>{{ $template['items_count'] }} items</span>
                                            @if($template['discount_amount'] > 0)
                                                <span class="text-green-600 dark:text-green-400">
                                                    {{ $template['discount_amount'] }}{{ $template['discount_type'] === 'percentage' ? '%' : '' }} off
                                                </span>
                                            @endif
                                        </div>
                                        <div>
                                            {{ $template['usage_count'] }} uses
                                        </div>
                                    </div>
                                </div>
                            </flux:card>
                        @endforeach
                    </div>
                </div>
            @endforeach
        </div>
    @else
        <!-- Empty State -->
        <flux:card class="p-12">
            <div class="text-center">
                <div class="text-gray-400 mb-4">
                    <svg class="w-16 h-16 mx-auto" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"/>
                    </svg>
                </div>
                <h3 class="text-lg font-medium text-gray-900 dark:text-white mb-2">No templates found</h3>
                <p class="text-gray-500 dark:text-gray-400 mb-6">
                    @if(!empty($searchQuery) || !empty($selectedCategory))
                        Try adjusting your search criteria or clearing the filters.
                    @else
                        No templates are available yet. Create your first template to get started.
                    @endif
                </p>
                @if(!empty($searchQuery) || !empty($selectedCategory))
                    <flux:button wire:click="clearFilters" variant="primary">
                        Clear Filters
                    </flux:button>
                @endif
            </div>
        </flux:card>
    @endif

    <!-- Template Preview Modal -->
    @if($showPreview && $previewTemplate)
        <div class="fixed inset-0 bg-gray-500/75 flex items-center justify-center z-50" wire:click.self="hidePreview">
            <flux:card class="w-full max-w-4xl max-h-[90vh] overflow-hidden">
                <div class="p-6">
                    <!-- Header -->
                    <div class="flex items-center justify-between mb-6">
                        <div>
                            <h3 class="text-lg font-medium text-gray-900 dark:text-white">
                                {{ $previewTemplate['name'] }}
                            </h3>
                            <p class="text-sm text-gray-600 dark:text-gray-400">
                                {{ $previewTemplate['category'] }} • {{ $previewTemplate['items_count'] }} items
                            </p>
                        </div>
                        <div class="flex items-center space-x-2">
                            <flux:button 
                                wire:click="selectTemplate({{ $previewTemplate['id'] }})"
                                variant="primary"
                                size="sm"
                            >
                                Use Template
                            </flux:button>
                            <flux:button 
                                wire:click="hidePreview"
                                variant="ghost"
                                size="sm"
                            >
                                <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"/>
                                </svg>
                            </flux:button>
                        </div>
                    </div>

                    <!-- Content -->
                    <div class="max-h-[70vh] overflow-y-auto space-y-6">
                        <!-- Description -->
                        @if($previewTemplate['description'])
                            <div>
                                <h4 class="font-medium text-gray-900 dark:text-white mb-2">Description</h4>
                                <p class="text-gray-600 dark:text-gray-400">{{ $previewTemplate['description'] }}</p>
                            </div>
                        @endif

                        <!-- Scope -->
                        @if($previewTemplate['scope'])
                            <div>
                                <h4 class="font-medium text-gray-900 dark:text-white mb-2">Scope of Work</h4>
                                <p class="text-gray-600 dark:text-gray-400">{{ $previewTemplate['scope'] }}</p>
                            </div>
                        @endif

                        <!-- Items Preview -->
                        @if(!empty($previewTemplate['items']))
                            <div>
                                <h4 class="font-medium text-gray-900 dark:text-white mb-2">Items ({{ count($previewTemplate['items']) }})</h4>
                                <div class="space-y-2">
                                    @foreach($previewTemplate['items'] as $item)
                                        <div class="p-3 bg-gray-50 dark:bg-gray-800 rounded-lg">
                                            <div class="flex items-center justify-between">
                                                <div>
                                                    <h5 class="font-medium text-gray-900 dark:text-white text-sm">
                                                        {{ $item['name'] }}
                                                    </h5>
                                                    @if($item['description'])
                                                        <p class="text-xs text-gray-500 dark:text-gray-400">
                                                            {{ $item['description'] }}
                                                        </p>
                                                    @endif
                                                </div>
                                                <div class="text-right text-sm">
                                                    <span class="text-gray-600 dark:text-gray-300">
                                                        {{ $item['quantity'] }} × ${{ number_format($item['unit_price'], 2) }}
                                                    </span>
                                                </div>
                                            </div>
                                        </div>
                                    @endforeach
                                </div>
                            </div>
                        @endif

                        <!-- Pricing -->
                        @if($previewTemplate['discount_amount'] > 0)
                            <div>
                                <h4 class="font-medium text-gray-900 dark:text-white mb-2">Pricing</h4>
                                <p class="text-gray-600 dark:text-gray-400">
                                    Default discount: {{ $previewTemplate['discount_amount'] }}{{ $previewTemplate['discount_type'] === 'percentage' ? '%' : '' }}
                                </p>
                            </div>
                        @endif

                        <!-- Terms -->
                        @if($previewTemplate['terms_conditions'])
                            <div>
                                <h4 class="font-medium text-gray-900 dark:text-white mb-2">Terms & Conditions</h4>
                                <p class="text-gray-600 dark:text-gray-400 text-sm">{{ $previewTemplate['terms_conditions'] }}</p>
                            </div>
                        @endif

                        <!-- Usage Stats -->
                        <div class="border-t border-gray-200 dark:border-gray-700 pt-4">
                            <div class="grid grid-cols-2 gap-4 text-sm">
                                <div>
                                    <span class="text-gray-500 dark:text-gray-400">Usage Count:</span>
                                    <span class="ml-2 font-medium text-gray-900 dark:text-white">{{ $previewTemplate['usage_count'] }}</span>
                                </div>
                                <div>
                                    <span class="text-gray-500 dark:text-gray-400">Last Updated:</span>
                                    <span class="ml-2 font-medium text-gray-900 dark:text-white">
                                        {{ \Carbon\Carbon::parse($previewTemplate['updated_at'])->format('M d, Y') }}
                                    </span>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </flux:card>
        </div>
    @endif
</div>