<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Asset Label - {{ $asset->name }}</title>
    <style>
        @page {
            size: 4in 2in;
            margin: 0;
        }
        
        body {
            margin: 0;
            padding: 10px;
            font-family: Arial, sans-serif;
            font-size: 12px;
            width: 4in;
            height: 2in;
            box-sizing: border-box;
        }
        
        .label-container {
            border: 2px solid #000;
            padding: 10px;
            height: 100%;
            display: flex;
            align-items: center;
            justify-content: space-between;
        }
        
        .label-info {
            flex: 1;
        }
        
        .label-info h3 {
            margin: 0 0 5px 0;
            font-size: 16px;
        }
        
        .label-info p {
            margin: 3px 0;
            font-size: 11px;
        }
        
        .label-info .asset-id {
            font-weight: bold;
            font-size: 14px;
        }
        
        .qr-code {
            width: 120px;
            height: 120px;
            margin-left: 10px;
        }
        
        .qr-code svg {
            width: 100%;
            height: 100%;
        }
        
        @media print {
            body {
                print-color-adjust: exact;
                -webkit-print-color-adjust: exact;
            }
        }
    </style>
</head>
<body onload="window.print()">
    <div class="label-container mx-auto mx-auto px-4 mx-auto px-6">
        <div class="label-info">
            <h3>{{ $asset->client->name }}</h3>
            <p class="asset-id">Asset ID: {{ $asset->id }}</p>
            <p><strong>{{ $asset->name }}</strong></p>
            <p>{{ $asset->type }} - {{ $asset->make }} {{ $asset->model }}</p>
            @if($asset->serial)
                <p>S/N: {{ $asset->serial }}</p>
            @endif
            @if($asset->location)
                <p>Location: {{ $asset->location->name }}</p>
            @endif
        </div>
        <div class="qr-code">
            {!! $qrCode !!}
        </div>
    </div>
</body>
</html>