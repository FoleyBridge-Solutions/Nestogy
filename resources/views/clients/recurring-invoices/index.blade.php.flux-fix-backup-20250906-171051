@extends('layouts.app')

@section('title', 'Client Recurring Invoices')

@section('content')
<div class="w-full px-6">
    <div class="flex flex-wrap -mx-4">
        <div class="flex-1 px-6-12">
            <div class="flex justify-between items-center mb-6">
                <h1 class="h3 mb-0">Client Recurring Invoices</h1>
                <div class="px-6 py-2 font-medium rounded-md transition-colors-group">
                    <a href="{{ route('clients.recurring-invoices.standalone.create') }}" class="inline-flex items-center px-6 py-2 bg-blue-600 text-white font-medium rounded-md hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-blue-500">
                        <i class="fas fa-plus mr-2"></i>Add New Recurring Invoice
                    </a>
                    <button type="button" class="btn px-6 py-2 font-medium rounded-md transition-colors-outline-secondary dropdown-toggle" x-data="{ open: false }" @click="open = !open">
                        <i class="fas fa-download"></i> Export
                    </flux:button>
                    <ul class="dropdown-menu">
                        <li><a class="dropdown-item" href="{{ route('clients.recurring-invoices.standalone.export', request()->query()) }}">
                            <i class="fas fa-file-csv mr-2"></i>Export to CSV
                        </a></li>
                    </ul>
                </div>
            </div>

            <!-- Filters -->
            <div class="bg-white rounded-lg shadow-md overflow-hidden mb-6">
                <div class="p-6">
                    <form method="GET" action="{{ route('clients.recurring-invoices.standalone.index') }}">
                        <div class="flex flex-wrap -mx-4 g-3">
                            <div class="md:w-1/4 px-6">
                                <label for="search" class="block text-sm font-medium text-gray-700 mb-1">Search</flux:label>
                                <input type="text" 
                                       class="block w-full px-6 py-2 border border-gray-300 rounded-md shadow-sm focus:outline-none focus:ring-blue-500 focus:border-blue-500 sm:text-sm" 
                                       id="search" 
                                       name="search" 
                                       value="{{ request('search') }}" 
                                       placeholder="Search template name, description...">
                            </div>
                            <div class="md:w-1/6 px-6">
                                <label for="client_id" class="block text-sm font-medium text-gray-700 mb-1">Client</flux:label>
                                <select name="client_id" id="client_id" class="block w-full px-6 py-2 border border-gray-300 rounded-md shadow-sm focus:outline-none focus:ring-blue-500 focus:border-blue-500 sm:text-sm">
                                    <option value="">All Clients</option>
                                    @foreach($clients as $client)
                                        <option value="{{ $client->id }}" {{ request('client_id') == $client->id ? 'selected' : '' }}>
                                            {{ $client->display_name }}
                                        </option>
                                    @endforeach
                                </flux:select>
                            </div>
                            <div class="flex-1 px-6-md-2">
                                <label for="status" class="block text-sm font-medium text-gray-700 dark:text-gray-300 mb-1">Status</flux:label>
                                <select name="status" id="status" class="block w-full px-6 py-2 border border-gray-300 rounded-md shadow-sm focus:outline-none focus:ring-blue-500 focus:border-blue-500 sm:text-sm">
                                    <option value="">All Statuses</option>
                                    @foreach($statuses as $key => $value)
                                        <option value="{{ $key }}" {{ request('status') == $key ? 'selected' : '' }}>
                                            {{ $value }}
                                        </option>
                                    @endforeach
                                </flux:select>
                            </div>
                            <div class="flex-1 px-6-md-2">
                                <label for="frequency" class="block text-sm font-medium text-gray-700 dark:text-gray-300 mb-1">Frequency</flux:label>
                                <select name="frequency" id="frequency" class="block w-full px-6 py-2 border border-gray-300 dark:border-gray-600 rounded-md shadow-sm focus:outline-none focus:ring-blue-500 focus:border-blue-500 sm:text-sm">
                                    <option value="">All Frequencies</option>
                                    @foreach($frequencies as $key => $value)
                                        <option value="{{ $key }}" {{ request('frequency') == $key ? 'selected' : '' }}>
                                            {{ $value }}
                                        </option>
                                    @endforeach
                                </flux:select>
                            </div>
                            <div class="flex-1 px-6-md-2">
                                <label for="currency" class="block text-sm font-medium text-gray-700 dark:text-gray-300 mb-1">Currency</flux:label>
                                <select name="currency" id="currency" class="block w-full px-6 py-2 border border-gray-300 dark:border-gray-600 rounded-md shadow-sm focus:outline-none focus:ring-blue-500 focus:border-blue-500 sm:text-sm">
                                    <option value="">All Currencies</option>
                                    @foreach($currencies as $key => $value)
                                        <option value="{{ $key }}" {{ request('currency') == $key ? 'selected' : '' }}>
                                            {{ $key }}
                                        </option>
                                    @endforeach
                                </flux:select>
                            </div>
                            <div class="flex-1 px-6-md-1">
                                <label class="block text-sm font-medium text-gray-700 dark:text-gray-300 mb-1 block">&nbsp;</flux:label>
                                <button type="submit" class="inline-flex items-center px-6 py-2 bg-blue-600 text-white font-medium rounded-md hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-blue-500">
                                    <i class="fas fa-search"></i>
                                </flux:button>
                            </div>
                        </div>

                        <div class="flex flex-wrap -mx-4 g-3 mt-2">
                            <div class="flex-1 px-6-md-2">
                                <label for="min_amount" class="block text-sm font-medium text-gray-700 dark:text-gray-300 mb-1">Min Amount</flux:label>
                                <input type="number" 
                                       class="block w-full px-6 py-2 border border-gray-300 rounded-md shadow-sm focus:outline-none focus:ring-blue-500 focus:border-blue-500 sm:text-sm" 
                                       id="min_amount" 
                                       name="min_amount" 
                                       value="{{ request('min_amount') }}" 
                                       step="0.01"
                                       placeholder="0.00">
                            </div>
                            <div class="flex-1 px-6-md-2">
                                <label for="max_amount" class="block text-sm font-medium text-gray-700 dark:text-gray-300 mb-1">Max Amount</flux:label>
                                <input type="number" 
                                       class="block w-full px-6 py-2 border border-gray-300 dark:border-gray-600 rounded-md shadow-sm focus:outline-none focus:ring-blue-500 focus:border-blue-500 sm:text-sm" 
                                       id="max_amount" 
                                       name="max_amount" 
                                       value="{{ request('max_amount') }}" 
                                       step="0.01"
                                       placeholder="0.00">
                            </div>
                            <div class="flex-1 px-6-md-2">
                                <label class="block text-sm font-medium text-gray-700 dark:text-gray-300 mb-1 block">&nbsp;</flux:label>
                                <div class="flex items-center">
                                    <input class="flex items-center-input" 
                                           type="checkbox" 
                                           name="due_soon" 
                                           id="due_soon" 
                                           value="1" 
                                           {{ request('due_soon') ? 'checked' : '' }}>
                                    <label class="flex items-center-label" for="due_soon">
                                        Due Soon (7 days)
                                    </flux:label>
                                </div>
                            </div>
                            <div class="flex-1 px-6-md-2">
                                <label class="block text-sm font-medium text-gray-700 dark:text-gray-300 mb-1 block">&nbsp;</flux:label>
                                <div class="flex items-center">
                                    <input class="flex items-center-input" 
                                           type="checkbox" 
                                           name="overdue" 
                                           id="overdue" 
                                           value="1" 
                                           {{ request('overdue') ? 'checked' : '' }}>
                                    <label class="flex items-center-label" for="overdue">
                                        Overdue
                                    </flux:label>
                                </div>
                            </div>
                            <div class="flex-1 px-6-md-4">
                                <label class="block text-sm font-medium text-gray-700 dark:text-gray-300 mb-1 block">&nbsp;</flux:label>
                                <a href="{{ route('clients.recurring-invoices.standalone.index') }}" class="btn px-6 py-2 font-medium rounded-md transition-colors-outline-secondary">
                                    <i class="fas fa-times"></i> Clear Filters
                                </a>
                            </div>
                        </div>
                    </form>
                </div>
            </div>

            <!-- Recurring Invoices Table -->
            <div class="bg-white rounded-lg shadow-md overflow-hidden">
                <div class="p-6">
                    @if($invoices->count() > 0)
                        <div class="min-w-full divide-y divide-gray-200-responsive">
                            <table class="min-w-full divide-y divide-gray-200 dark:divide-gray-700 min-w-full divide-y divide-gray-200-striped [&>tbody>tr:hover]:bg-gray-100">
                                <thead class="min-w-full divide-y divide-gray-200 dark:divide-gray-700-dark">
                                    <tr>
                                        <th>Invoice Details</th>
                                        <th>Client</th>
                                        <th>Amount</th>
                                        <th>Frequency</th>
                                        <th>Next Invoice</th>
                                        <th>Status</th>
                                        <th>Performance</th>
                                        <th>Actions</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    @foreach($invoices as $invoice)
                                        <tr>
                                            <td>
                                                <div>
                                                    <div class="font-bold">{{ $invoice->template_name }}</div>
                                                    @if($invoice->description)
                                                        <small class="text-gray-600">{{ Str::limit($invoice->description, 60) }}</small>
                                                    @endif
                                                    <div class="small text-gray-600 mt-1">
                                                        <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-gray-100 text-dark">{{ $invoice->invoice_prefix }}</span>
                                                        @if($invoice->auto_send)
                                                            <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-info">Auto Send</span>
                                                        @endif
                                                    </div>
                                                </div>
                                            </td>
                                            <td>
                                                <a href="{{ route('clients.show', $invoice->client) }}" class="text-decoration-none">
                                                    {{ $invoice->client->display_name }}
                                                </a>
                                            </td>
                                            <td>
                                                <div>
                                                    <div class="font-bold">{{ $invoice->formatted_total_amount }}</div>
                                                    @if($invoice->tax_rate > 0)
                                                        <small class="text-gray-600 dark:text-gray-400">
                                                            {{ $invoice->formatted_amount }} + {{ $invoice->tax_rate }}% tax
                                                        </small>
                                                    @endif
                                                    <div class="small text-gray-600 dark:text-gray-400">{{ $invoice->currency }}</div>
                                                </div>
                                            </td>
                                            <td>
                                                <div class="small">
                                                    <div class="font-bold">{{ $invoice->frequency_description }}</div>
                                                    @if($invoice->payment_terms_days)
                                                        <div class="text-gray-600 dark:text-gray-400">{{ $invoice->payment_terms_days }} day terms</div>
                                                    @endif
                                                </div>
                                            </td>
                                            <td>
                                                <div class="small">
                                                    @if($invoice->next_invoice_date)
                                                        <div class="font-bold">{{ $invoice->next_invoice_date->format('M j, Y') }}</div>
                                                        @if($invoice->days_until_next_invoice !== null)
                                                            @if($invoice->days_until_next_invoice < 0)
                                                                <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-danger">{{ abs($invoice->days_until_next_invoice) }} days overdue</span>
                                                            @elseif($invoice->days_until_next_invoice == 0)
                                                                <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-warning">Due Today</span>
                                                            @elseif($invoice->days_until_next_invoice <= 7)
                                                                <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-info">{{ $invoice->days_until_next_invoice }} days</span>
                                                            @else
                                                                <span class="text-gray-600 dark:text-gray-400">{{ $invoice->days_until_next_invoice }} days</span>
                                                            @endif
                                                        @endif
                                                    @else
                                                        <span class="text-gray-600 dark:text-gray-400">Not scheduled</span>
                                                    @endif
                                                </div>
                                            </td>
                                            <td>
                                                @switch($invoice->status)
                                                    @case('draft')
                                                        <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-gray-600">Draft</span>
                                                        @break
                                                    @case('active')
                                                        <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-success">Active</span>
                                                        @break
                                                    @case('paused')
                                                        <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-warning">Paused</span>
                                                        @break
                                                    @case('cancelled')
                                                        <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-danger">Cancelled</span>
                                                        @break
                                                    @case('expired')
                                                        <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-gray-900">Expired</span>
                                                        @break
                                                    @default
                                                        <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-gray-600">{{ ucfirst($invoice->status) }}</span>
                                                @endswitch
                                            </td>
                                            <td>
                                                <div class="small">
                                                    <div class="flex justify-between">
                                                        <span>Invoices:</span>
                                                        <span class="font-bold">{{ $invoice->invoice_count ?? 0 }}</span>
                                                    </div>
                                                    <div class="flex justify-between">
                                                        <span>Revenue:</span>
                                                        <span class="font-bold text-green-600">{{ $invoice->getCurrencySymbol() }}{{ number_format($invoice->total_revenue ?? 0, 0) }}</span>
                                                    </div>
                                                </div>
                                            </td>
                                            <td>
                                                <div class="btn-group px-6 py-2 font-medium rounded-md transition-colors-group-sm">
                                                    <a href="{{ route('clients.recurring-invoices.standalone.show', $invoice) }}" 
                                                       class="btn px-6 py-2 font-medium rounded-md transition-colors-outline-primary" 
                                                       title="View">
                                                        <i class="fas fa-eye"></i>
                                                    </a>
                                                    @if($invoice->isActive())
                                                        <a href="{{ route('clients.recurring-invoices.standalone.edit', $invoice) }}" 
                                                           class="btn px-6 py-2 font-medium rounded-md transition-colors-outline-secondary" 
                                                           title="Edit">
                                                            <i class="fas fa-edit"></i>
                                                        </a>
                                                        @if($invoice->isDue())
                                                            <button type="button" 
                                                                    class="btn px-6 py-2 font-medium rounded-md transition-colors-outline-success" 
                                                                    title="Generate Invoice"
                                                                    onclick="generateInvoice({{ $invoice->id }})">
                                                                <i class="fas fa-file-invoice"></i>
                                                            </flux:button>
                                                        @endif
                                                    @endif
                                                    <button type="button" 
                                                            class="btn px-6 py-2 font-medium rounded-md transition-colors-outline-danger" 
                                                            title="Delete"
                                                            onclick="deleteInvoice({{ $invoice->id }})">
                                                        <i class="fas fa-trash"></i>
                                                    </flux:button>
                                                </div>
                                            </td>
                                        </tr>
                                    @endforeach
                                </tbody>
                            </table>
                        </div>

                        <!-- Pagination -->
                        <div class="flex justify-between items-center mt-6">
                            <div class="text-gray-600 dark:text-gray-400 small">
                                Showing {{ $invoices->firstItem() }} to {{ $invoices->lastItem() }} of {{ $invoices->total() }} results
                            </div>
                            {{ $invoices->links() }}
                        </div>
                    @else
                        <div class="text-center py-8">
                            <i class="fas fa-file-invoice fa-3x text-gray-600 dark:text-gray-400 mb-6"></i>
                            <h5>No recurring invoices found</h5>
                            <p class="text-gray-600 dark:text-gray-400">Get started by creating your first recurring invoice.</p>
                            <a href="{{ route('clients.recurring-invoices.standalone.create') }}" class="btn px-6 py-2 font-medium rounded-md transition-colors-primary">
                                <i class="fas fa-plus mr-2"></i>Add New Recurring Invoice
                            </a>
                        </div>
                    @endif
                </div>
            </div>
        </div>
    </div>
</div>

<!-- Delete Confirmation Modal -->
<div class="fixed inset-0 z-50 overflow-y-auto fade" id="deleteInvoiceModal" tabindex="-1">
    <div class="fixed inset-0 z-50 overflow-y-auto-dialog">
        <div class="fixed inset-0 z-50 overflow-y-auto-content">
            <div class="fixed inset-0 z-50 overflow-y-auto-header">
                <h5 class="fixed inset-0 z-50 overflow-y-auto-title">Delete Recurring Invoice</h5>
                <button type="button" class="px-6 py-2 font-medium rounded-md transition-colors-close" @click="$dispatch('close-modal')"></flux:button>
            </div>
            <div class="fixed inset-0 z-50 overflow-y-auto-body">
                Are you sure you want to delete this recurring invoice? This action cannot be undone.
            </div>
            <div class="fixed inset-0 z-50 overflow-y-auto-footer">
                <button type="button" class="inline-flex items-center px-6 py-2 bg-gray-600 text-white font-medium rounded-md hover:bg-gray-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-gray-500" @click="$dispatch('close-modal')">Cancel</flux:button>
                <form id="deleteInvoiceForm" method="POST" class="inline">
                    @csrf
                    @method('DELETE')
                    <button type="submit" class="inline-flex items-center px-6 py-2 bg-red-600 text-white font-medium rounded-md hover:bg-red-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-red-500">Delete Invoice</flux:button>
                </form>
            </div>
        </div>
    </div>
</div>

<!-- Generate Invoice Modal -->
<div class="fixed inset-0 z-50 overflow-y-auto fade" id="generateInvoiceModal" tabindex="-1">
    <div class="fixed inset-0 z-50 overflow-y-auto-dialog">
        <div class="fixed inset-0 z-50 overflow-y-auto-content">
            <div class="fixed inset-0 z-50 overflow-y-auto-header">
                <h5 class="fixed inset-0 z-50 overflow-y-auto-title">Generate Invoice</h5>
                <button type="button" class="px-6 py-2 font-medium rounded-md transition-colors-close" @click="$dispatch('close-modal')"></flux:button>
            </div>
            <div class="fixed inset-0 z-50 overflow-y-auto-body">
                Are you sure you want to generate an invoice from this recurring invoice template?
            </div>
            <div class="fixed inset-0 z-50 overflow-y-auto-footer">
                <button type="button" class="inline-flex items-center px-6 py-2 bg-gray-600 text-white font-medium rounded-md hover:bg-gray-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-gray-500" @click="$dispatch('close-modal')">Cancel</flux:button>
                <form id="generateInvoiceForm" method="POST" class="inline">
                    @csrf
                    <button type="submit" class="btn px-6 py-2 font-medium rounded-md transition-colors-primary">Generate Invoice</flux:button>
                </form>
            </div>
        </div>
    </div>
</div>
@endsection

@push('scripts')
<script>
function deleteInvoice(invoiceId) {
    const form = document.getElementById('deleteInvoiceForm');
    form.action = '/clients/recurring-invoices/' + invoiceId;
    
    const modal = new bootstrap.Modal(document.getElementById('deleteInvoiceModal'));
    modal.show();
}

function generateInvoice(invoiceId) {
    const form = document.getElementById('generateInvoiceForm');
    form.action = '/clients/recurring-invoices/' + invoiceId + '/generate';
    
    const modal = new bootstrap.Modal(document.getElementById('generateInvoiceModal'));
    modal.show();
}
</script>
@endpush