@extends('layouts.app')

@section('title', 'Recurring Invoice Details')

@section('content')
<div class="w-full px-6">
    <div class="flex flex-wrap -mx-4">
        <div class="flex-1 px-6-12">
            <div class="flex justify-between items-center mb-6">
                <h1 class="h3 mb-0">Recurring Invoice Details</h1>
                <div class="px-6 py-2 font-medium rounded-md transition-colors-group">
                    @if($recurringInvoice->isActive())
                        <a href="{{ route('clients.recurring-invoices.standalone.edit', $recurringInvoice) }}" class="inline-flex items-center px-6 py-2 bg-blue-600 text-white font-medium rounded-md hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-blue-500">
                            <i class="fas fa-edit mr-2"></i>Edit Invoice
                        </a>
                        @if($recurringInvoice->isDue())
                            <button type="button" class="inline-flex items-center px-6 py-2 bg-green-600 text-white font-medium rounded-md hover:bg-green-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-green-500" onclick="generateInvoice({{ $recurringInvoice->id }})">
                                <i class="fas fa-file-invoice mr-2"></i>Generate Invoice
                            </flux:button>
                        @endif
                    @endif
                    <a href="{{ route('clients.recurring-invoices.standalone.index') }}" class="btn px-6 py-2 font-medium rounded-md transition-colors-outline-secondary">
                        <i class="fas fa-arrow-left mr-2"></i>Back to Invoices
                    </a>
                    <button type="button" 
                            class="btn px-6 py-2 font-medium rounded-md transition-colors-outline-danger" 
                            onclick="deleteInvoice({{ $recurringInvoice->id }})">
                        <i class="fas fa-trash mr-2"></i>Delete
                    </flux:button>
                </div>
            </div>

            <div class="flex flex-wrap -mx-4">
                <div class="flex-1 px-6-lg-8">
                    <!-- Main Invoice Details -->
                    <div class="bg-white rounded-lg shadow-md overflow-hidden mb-6">
                        <div class="px-6 py-6 border-b border-gray-200 bg-gray-50">
                            <div class="flex justify-between items-center">
                                <h5 class="bg-white rounded-lg shadow-md overflow-hidden-title mb-0">
                                    <i class="fas fa-file-invoice mr-2"></i>{{ $recurringInvoice->template_name }}
                                </h5>
                                <div class="flex gap-2">
                                    @switch($recurringInvoice->status)
                                        @case('draft')
                                            <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-gray-600">Draft</span>
                                            @break
                                        @case('active')
                                            <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-success">Active</span>
                                            @break
                                        @case('paused')
                                            <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-warning">Paused</span>
                                            @break
                                        @case('cancelled')
                                            <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-danger">Cancelled</span>
                                            @break
                                        @case('expired')
                                            <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-gray-900">Expired</span>
                                            @break
                                    @endswitch

                                    @if($recurringInvoice->auto_send)
                                        <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-info">Auto Send</span>
                                    @endif
                                </div>
                            </div>
                        </div>
                        <div class="p-6">
                            <div class="flex flex-wrap -mx-4">
                                <div class="md:w-1/2 px-6">
                                    <h6 class="text-gray-600">Client</h6>
                                    <p class="mb-6">
                                        <a href="{{ route('clients.show', $recurringInvoice->client) }}" class="text-decoration-none">
                                            {{ $recurringInvoice->client->display_name }}
                                        </a>
                                    </p>

                                    <h6 class="text-gray-600">Amount Details</h6>
                                    <div class="mb-6">
                                        <div class="flex justify-between">
                                            <span>Subtotal:</span>
                                            <span class="font-bold">{{ $recurringInvoice->formatted_amount }}</span>
                                        </div>
                                        @if($recurringInvoice->tax_rate > 0)
                                            <div class="flex justify-between">
                                                <span>Tax ({{ $recurringInvoice->tax_rate }}%):</span>
                                                <span>{{ $recurringInvoice->getCurrencySymbol() }}{{ number_format($recurringInvoice->tax_amount, 2) }}</span>
                                            </div>
                                        @endif
                                        <hr class="my-2">
                                        <div class="flex justify-between">
                                            <span class="font-bold">Total:</span>
                                            <span class="font-bold text-blue-600 fs-5">{{ $recurringInvoice->formatted_total_amount }}</span>
                                        </div>
                                    </div>

                                    <h6 class="text-gray-600 dark:text-gray-400">Currency</h6>
                                    <p class="mb-6">{{ $recurringInvoice->currency }} ({{ $recurringInvoice->getCurrencySymbol() }})</p>
                                </div>

                                <div class="md:w-1/2 px-6">
                                    <h6 class="text-gray-600 dark:text-gray-400">Frequency</h6>
                                    <p class="mb-6">{{ $recurringInvoice->frequency_description }}</p>

                                    <h6 class="text-gray-600 dark:text-gray-400">Schedule</h6>
                                    <div class="mb-6">
                                        <div class="flex justify-between mb-1">
                                            <span>Start Date:</span>
                                            <span>{{ $recurringInvoice->start_date->format('M j, Y') }}</span>
                                        </div>
                                        @if($recurringInvoice->end_date)
                                            <div class="flex justify-between mb-1">
                                                <span>End Date:</span>
                                                <span>{{ $recurringInvoice->end_date->format('M j, Y') }}</span>
                                            </div>
                                        @endif
                                        @if($recurringInvoice->next_invoice_date)
                                            <div class="flex justify-between mb-1">
                                                <span>Next Invoice:</span>
                                                <span class="font-bold">{{ $recurringInvoice->next_invoice_date->format('M j, Y') }}</span>
                                            </div>
                                            @if($recurringInvoice->days_until_next_invoice !== null)
                                                <div class="mt-2">
                                                    @if($recurringInvoice->days_until_next_invoice < 0)
                                                        <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-danger">{{ abs($recurringInvoice->days_until_next_invoice) }} days overdue</span>
                                                    @elseif($recurringInvoice->days_until_next_invoice == 0)
                                                        <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-warning">Due Today</span>
                                                    @elseif($recurringInvoice->days_until_next_invoice <= 7)
                                                        <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-info">{{ $recurringInvoice->days_until_next_invoice }} days to go</span>
                                                    @else
                                                        <span class="text-gray-600 dark:text-gray-400">{{ $recurringInvoice->days_until_next_invoice }} days to go</span>
                                                    @endif
                                                </div>
                                            @endif
                                        @endif
                                    </div>

                                    <h6 class="text-gray-600 dark:text-gray-400">Payment Terms</h6>
                                    <p class="mb-6">{{ $recurringInvoice->payment_terms_days }} days</p>
                                </div>
                            </div>

                            @if($recurringInvoice->description)
                                <hr>
                                <h6 class="text-gray-600 dark:text-gray-400">Description</h6>
                                <div class="mb-6">
                                    {!! nl2br(e($recurringInvoice->description)) !!}
                                </div>
                            @endif

                            @if($recurringInvoice->line_items && count($recurringInvoice->line_items) > 0)
                                <hr>
                                <h6 class="text-gray-600 dark:text-gray-400">Line Items</h6>
                                <div class="mb-6">
                                    <ul class="list-unstyled">
                                        @foreach($recurringInvoice->line_items as $item)
                                            <li class="mb-1">
                                                <i class="fas fa-check text-green-600 mr-2"></i>
                                                {{ $item['description'] }}
                                            </li>
                                        @endforeach
                                    </ul>
                                </div>
                            @endif

                            @if($recurringInvoice->invoice_notes)
                                <hr>
                                <h6 class="text-gray-600 dark:text-gray-400">Invoice Notes</h6>
                                <div class="mb-6 bg-gray-100 p-6 rounded">
                                    {!! nl2br(e($recurringInvoice->invoice_notes)) !!}
                                </div>
                            @endif

                            @if($recurringInvoice->payment_instructions)
                                <hr>
                                <h6 class="text-gray-600 dark:text-gray-400">Payment Instructions</h6>
                                <div class="bg-gray-100 p-6 rounded">
                                    {!! nl2br(e($recurringInvoice->payment_instructions)) !!}
                                </div>
                            @endif
                        </div>
                    </div>

                    <!-- Late Fee Settings -->
                    @if($recurringInvoice->late_fee_percentage > 0 || $recurringInvoice->late_fee_flat_amount > 0)
                        <div class="bg-white dark:bg-gray-800 rounded-lg shadow-md overflow-hidden mb-6">
                            <div class="px-6 py-6 border-b border-gray-200 bg-gray-50">
                                <h5 class="bg-white dark:bg-gray-800 rounded-lg shadow-md overflow-hidden-title mb-0">
                                    <i class="fas fa-exclamation-triangle mr-2"></i>Late Fee Settings
                                </h5>
                            </div>
                            <div class="p-6">
                                <div class="flex flex-wrap -mx-4">
                                    @if($recurringInvoice->late_fee_percentage > 0)
                                        <div class="flex-1 px-6-md-6">
                                            <h6 class="text-gray-600 dark:text-gray-400">Percentage Fee</h6>
                                            <p class="mb-0">{{ $recurringInvoice->late_fee_percentage }}% of total amount</p>
                                        </div>
                                    @endif
                                    @if($recurringInvoice->late_fee_flat_amount > 0)
                                        <div class="flex-1 px-6-md-6">
                                            <h6 class="text-gray-600 dark:text-gray-400">Flat Fee</h6>
                                            <p class="mb-0">{{ $recurringInvoice->getCurrencySymbol() }}{{ number_format($recurringInvoice->late_fee_flat_amount, 2) }}</p>
                                        </div>
                                    @endif
                                </div>
                            </div>
                        </div>
                    @endif
                </div>

                <div class="flex-1 px-6-lg-4">
                    <!-- Performance Metrics -->
                    <div class="bg-white dark:bg-gray-800 rounded-lg shadow-md overflow-hidden mb-6">
                        <div class="bg-white dark:bg-gray-800 rounded-lg shadow-md overflow-hidden-header">
                            <h5 class="bg-white dark:bg-gray-800 rounded-lg shadow-md overflow-hidden-title mb-0">
                                <i class="fas fa-chart-bar mr-2"></i>Performance
                            </h5>
                        </div>
                        <div class="bg-white dark:bg-gray-800 rounded-lg shadow-md overflow-hidden-body">
                            <div class="flex flex-wrap -mx-4 g-2 mb-6">
                                <div class="flex-1 px-6-6">
                                    <div class="text-center p-2 bg-light rounded">
                                        <div class="font-bold text-blue-600">{{ $recurringInvoice->invoice_count ?? 0 }}</div>
                                        <small class="text-gray-600 dark:text-gray-400">Invoices</small>
                                    </div>
                                </div>
                                <div class="flex-1 px-6-6">
                                    <div class="text-center p-2 bg-light rounded">
                                        <div class="font-bold text-green-600">{{ $recurringInvoice->getCurrencySymbol() }}{{ number_format($recurringInvoice->total_revenue ?? 0, 0) }}</div>
                                        <small class="text-gray-600 dark:text-gray-400">Revenue</small>
                                    </div>
                                </div>
                            </div>

                            @if($recurringInvoice->invoice_count > 0)
                                <div class="text-center p-2 bg-light rounded">
                                    <div class="font-bold text-cyan-600 dark:text-cyan-400">{{ $recurringInvoice->getCurrencySymbol() }}{{ number_format(($recurringInvoice->total_revenue ?? 0) / $recurringInvoice->invoice_count, 2) }}</div>
                                    <small class="text-gray-600 dark:text-gray-400">Avg per Invoice</small>
                                </div>
                            @endif
                        </div>
                    </div>

                    <!-- Invoice Information -->
                    <div class="bg-white dark:bg-gray-800 rounded-lg shadow-md overflow-hidden mb-6">
                        <div class="bg-white dark:bg-gray-800 rounded-lg shadow-md overflow-hidden-header">
                            <h5 class="bg-white dark:bg-gray-800 rounded-lg shadow-md overflow-hidden-title mb-0">
                                <i class="fas fa-info-circle mr-2"></i>Invoice Information
                            </h5>
                        </div>
                        <div class="bg-white dark:bg-gray-800 rounded-lg shadow-md overflow-hidden-body">
                            <div class="small text-gray-600 dark:text-gray-400">
                                <div class="flex justify-between mb-2">
                                    <span>Prefix:</span>
                                    <span class="font-bold">{{ $recurringInvoice->invoice_prefix }}</span>
                                </div>
                                <div class="flex justify-between mb-2">
                                    <span>Auto Send:</span>
                                    <span>{{ $recurringInvoice->auto_send ? 'Yes' : 'No' }}</span>
                                </div>
                                <div class="flex justify-between mb-2">
                                    <span>Created:</span>
                                    <span>{{ $recurringInvoice->created_at->format('M j, Y') }}</span>
                                </div>
                                @if($recurringInvoice->creator)
                                    <div class="flex justify-between mb-2">
                                        <span>Created by:</span>
                                        <span>{{ $recurringInvoice->creator->name }}</span>
                                    </div>
                                @endif
                                @if($recurringInvoice->last_invoice_date)
                                    <div class="flex justify-between">
                                        <span>Last Invoice:</span>
                                        <span>{{ $recurringInvoice->last_invoice_date->format('M j, Y') }}</span>
                                    </div>
                                @endif
                            </div>
                        </div>
                    </div>

                    <!-- Quick Actions -->
                    <div class="bg-white dark:bg-gray-800 rounded-lg shadow-md overflow-hidden mb-6">
                        <div class="bg-white dark:bg-gray-800 rounded-lg shadow-md overflow-hidden-header">
                            <h5 class="bg-white dark:bg-gray-800 rounded-lg shadow-md overflow-hidden-title mb-0">
                                <i class="fas fa-bolt mr-2"></i>Quick Actions
                            </h5>
                        </div>
                        <div class="bg-white dark:bg-gray-800 rounded-lg shadow-md overflow-hidden-body">
                            <div class="grid gap-2">
                                @if($recurringInvoice->isActive())
                                    @if($recurringInvoice->isDue())
                                        <button class="inline-flex items-center px-4 py-2 bg-green-600 text-white font-medium rounded-md hover:bg-green-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-green-500 px-6 py-2 font-medium rounded-md transition-colors-sm" onclick="generateInvoice({{ $recurringInvoice->id }})">
                                            <i class="fas fa-file-invoice mr-2"></i>Generate Invoice Now
                                        </flux:button>
                                    @endif
                                    
                                    <button class="btn bg-yellow-500 text-white hover:bg-yellow-600 px-6 py-2 font-medium rounded-md transition-colors-sm" onclick="pauseInvoice({{ $recurringInvoice->id }})">
                                        <i class="fas fa-pause mr-2"></i>Pause Invoice
                                    </flux:button>
                                    
                                    <button class="inline-flex items-center px-4 py-2 bg-red-600 text-white font-medium rounded-md hover:bg-red-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-red-500 px-6 py-2 font-medium rounded-md transition-colors-sm" onclick="cancelInvoice({{ $recurringInvoice->id }})">
                                        <i class="fas fa-times mr-2"></i>Cancel Invoice
                                    </flux:button>
                                @elseif($recurringInvoice->isPaused())
                                    <button class="btn bg-green-600 text-white hover:bg-green-700 px-6 py-2 font-medium rounded-md transition-colors-sm" onclick="resumeInvoice({{ $recurringInvoice->id }})">
                                        <i class="fas fa-play mr-2"></i>Resume Invoice
                                    </flux:button>
                                @endif

                                <hr class="my-2">
                                
                                <a href="{{ route('clients.recurring-invoices.standalone.create', ['client_id' => $recurringInvoice->client_id]) }}" 
                                   class="btn btn-outline-info px-6 py-2 font-medium rounded-md transition-colors-sm">
                                    <i class="fas fa-plus mr-2"></i>New Invoice for Client
                                </a>
                                
                                <a href="{{ route('clients.show', $recurringInvoice->client) }}" 
                                   class="btn border border-gray-600 text-gray-600 hover:bg-gray-50 px-6 py-2 font-medium rounded-md transition-colors-sm">
                                    <i class="fas fa-user mr-2"></i>View Client
                                </a>
                            </div>
                        </div>
                    </div>

                    <!-- Status History -->
                    @if($recurringInvoice->isPaused() || $recurringInvoice->isCancelled())
                        <div class="bg-white dark:bg-gray-800 rounded-lg shadow-md overflow-hidden">
                            <div class="bg-white dark:bg-gray-800 rounded-lg shadow-md overflow-hidden-header">
                                <h5 class="bg-white dark:bg-gray-800 rounded-lg shadow-md overflow-hidden-title mb-0">
                                    <i class="fas fa-history mr-2"></i>Status History
                                </h5>
                            </div>
                            <div class="bg-white dark:bg-gray-800 rounded-lg shadow-md overflow-hidden-body">
                                <div class="small text-gray-600 dark:text-gray-400">
                                    @if($recurringInvoice->isPaused())
                                        <div class="mb-2">
                                            <strong>Paused:</strong> {{ $recurringInvoice->paused_at->format('M j, Y g:i A') }}
                                            @if($recurringInvoice->paused_reason)
                                                <div class="mt-1">
                                                    <strong>Reason:</strong> {{ $recurringInvoice->paused_reason }}
                                                </div>
                                            @endif
                                        </div>
                                    @endif
                                    
                                    @if($recurringInvoice->isCancelled())
                                        <div>
                                            <strong>Cancelled:</strong> {{ $recurringInvoice->cancelled_at->format('M j, Y g:i A') }}
                                            @if($recurringInvoice->cancelled_reason)
                                                <div class="mt-1">
                                                    <strong>Reason:</strong> {{ $recurringInvoice->cancelled_reason }}
                                                </div>
                                            @endif
                                        </div>
                                    @endif
                                </div>
                            </div>
                        </div>
                    @endif
                </div>
            </div>
        </div>
    </div>
</div>

<!-- Delete Confirmation Modal -->
<div class="fixed inset-0 z-50 overflow-y-auto fade" id="deleteInvoiceModal" tabindex="-1">
    <div class="fixed inset-0 z-50 overflow-y-auto-dialog">
        <div class="fixed inset-0 z-50 overflow-y-auto-content">
            <div class="fixed inset-0 z-50 overflow-y-auto-header">
                <h5 class="fixed inset-0 z-50 overflow-y-auto-title">Delete Recurring Invoice</h5>
                <button type="button" class="px-6 py-2 font-medium rounded-md transition-colors-close" @click="$dispatch('close-modal')"></flux:button>
            </div>
            <div class="fixed inset-0 z-50 overflow-y-auto-body">
                Are you sure you want to delete this recurring invoice? This action cannot be undone.
            </div>
            <div class="fixed inset-0 z-50 overflow-y-auto-footer">
                <button type="button" class="inline-flex items-center px-6 py-2 bg-gray-600 text-white font-medium rounded-md hover:bg-gray-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-gray-500" @click="$dispatch('close-modal')">Cancel</flux:button>
                <form id="deleteInvoiceForm" method="POST" class="inline">
                    @csrf
                    @method('DELETE')
                    <button type="submit" class="inline-flex items-center px-6 py-2 bg-red-600 text-white font-medium rounded-md hover:bg-red-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-red-500">Delete Invoice</flux:button>
                </form>
            </div>
        </div>
    </div>
</div>

<!-- Generate Invoice Modal -->
<div class="fixed inset-0 z-50 overflow-y-auto fade" id="generateInvoiceModal" tabindex="-1">
    <div class="fixed inset-0 z-50 overflow-y-auto-dialog">
        <div class="fixed inset-0 z-50 overflow-y-auto-content">
            <div class="fixed inset-0 z-50 overflow-y-auto-header">
                <h5 class="fixed inset-0 z-50 overflow-y-auto-title">Generate Invoice</h5>
                <button type="button" class="px-6 py-2 font-medium rounded-md transition-colors-close" @click="$dispatch('close-modal')"></flux:button>
            </div>
            <div class="fixed inset-0 z-50 overflow-y-auto-body">
                Generate an invoice from this recurring invoice template?
            </div>
            <div class="fixed inset-0 z-50 overflow-y-auto-footer">
                <button type="button" class="inline-flex items-center px-6 py-2 bg-gray-600 text-white font-medium rounded-md hover:bg-gray-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-gray-500" @click="$dispatch('close-modal')">Cancel</flux:button>
                <form id="generateInvoiceForm" method="POST" class="inline">
                    @csrf
                    <button type="submit" class="inline-flex items-center px-6 py-2 bg-blue-600 text-white font-medium rounded-md hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-blue-500">Generate Invoice</flux:button>
                </form>
            </div>
        </div>
    </div>
</div>

<!-- Pause Invoice Modal -->
<div class="fixed inset-0 z-50 overflow-y-auto fade" id="pauseInvoiceModal" tabindex="-1">
    <div class="fixed inset-0 z-50 overflow-y-auto-dialog">
        <div class="fixed inset-0 z-50 overflow-y-auto-content">
            <div class="fixed inset-0 z-50 overflow-y-auto-header">
                <h5 class="fixed inset-0 z-50 overflow-y-auto-title">Pause Recurring Invoice</h5>
                <button type="button" class="px-6 py-2 font-medium rounded-md transition-colors-close" @click="$dispatch('close-modal')"></flux:button>
            </div>
            <form id="pauseInvoiceForm" method="POST">
                @csrf
                <div class="fixed inset-0 z-50 overflow-y-auto-body">
                    <p>Are you sure you want to pause this recurring invoice?</p>
                    <div class="mb-6">
                        <label for="pauseReason" class="block text-sm font-medium text-gray-700 mb-1">Reason (Optional)</flux:label>
                        <textarea name="reason" id="pauseReason" class="block w-full px-6 py-2 border border-gray-300 rounded-md shadow-sm focus:outline-none focus:ring-blue-500 focus:border-blue-500 sm:text-sm" rows="3" placeholder="Reason for pausing..."></flux:textarea>
                    </div>
                </div>
                <div class="fixed inset-0 z-50 overflow-y-auto-footer">
                    <button type="button" class="btn px-6 py-2 font-medium rounded-md transition-colors-secondary" @click="$dispatch('close-modal')">Cancel</flux:button>
                    <button type="submit" class="btn px-6 py-2 font-medium rounded-md transition-colors-warning">Pause Invoice</flux:button>
                </div>
            </form>
        </div>
    </div>
</div>

<!-- Resume Invoice Modal -->
<div class="fixed inset-0 z-50 overflow-y-auto fade" id="resumeInvoiceModal" tabindex="-1">
    <div class="fixed inset-0 z-50 overflow-y-auto-dialog">
        <div class="fixed inset-0 z-50 overflow-y-auto-content">
            <div class="fixed inset-0 z-50 overflow-y-auto-header">
                <h5 class="fixed inset-0 z-50 overflow-y-auto-title">Resume Recurring Invoice</h5>
                <button type="button" class="px-6 py-2 font-medium rounded-md transition-colors-close" @click="$dispatch('close-modal')"></flux:button>
            </div>
            <div class="fixed inset-0 z-50 overflow-y-auto-body">
                Resume this recurring invoice? It will start generating invoices according to its schedule.
            </div>
            <div class="fixed inset-0 z-50 overflow-y-auto-footer">
                <button type="button" class="btn px-6 py-2 font-medium rounded-md transition-colors-secondary" @click="$dispatch('close-modal')">Cancel</flux:button>
                <form id="resumeInvoiceForm" method="POST" class="inline">
                    @csrf
                    <button type="submit" class="btn px-6 py-2 font-medium rounded-md transition-colors-success">Resume Invoice</flux:button>
                </form>
            </div>
        </div>
    </div>
</div>

<!-- Cancel Invoice Modal -->
<div class="fixed inset-0 z-50 overflow-y-auto fade" id="cancelInvoiceModal" tabindex="-1">
    <div class="fixed inset-0 z-50 overflow-y-auto-dialog">
        <div class="fixed inset-0 z-50 overflow-y-auto-content">
            <div class="fixed inset-0 z-50 overflow-y-auto-header">
                <h5 class="fixed inset-0 z-50 overflow-y-auto-title">Cancel Recurring Invoice</h5>
                <button type="button" class="px-6 py-2 font-medium rounded-md transition-colors-close" @click="$dispatch('close-modal')"></flux:button>
            </div>
            <form id="cancelInvoiceForm" method="POST">
                @csrf
                <div class="fixed inset-0 z-50 overflow-y-auto-body">
                    <p>Are you sure you want to cancel this recurring invoice? This action cannot be undone.</p>
                    <div class="mb-6">
                        <label for="cancelReason" class="block text-sm font-medium text-gray-700 mb-1">Reason (Optional)</flux:label>
                        <textarea name="reason" id="cancelReason" class="block w-full px-6 py-2 border border-gray-300 rounded-md shadow-sm focus:outline-none focus:ring-blue-500 focus:border-blue-500 sm:text-sm" rows="3" placeholder="Reason for cancelling..."></flux:textarea>
                    </div>
                </div>
                <div class="fixed inset-0 z-50 overflow-y-auto-footer">
                    <button type="button" class="btn px-6 py-2 font-medium rounded-md transition-colors-secondary" @click="$dispatch('close-modal')">Cancel</flux:button>
                    <button type="submit" class="btn px-6 py-2 font-medium rounded-md transition-colors-danger">Cancel Invoice</flux:button>
                </div>
            </form>
        </div>
    </div>
</div>
@endsection

@push('scripts')
<script>
function deleteInvoice(invoiceId) {
    const form = document.getElementById('deleteInvoiceForm');
    form.action = '/clients/recurring-invoices/' + invoiceId;
    
    const modal = new bootstrap.Modal(document.getElementById('deleteInvoiceModal'));
    modal.show();
}

function generateInvoice(invoiceId) {
    const form = document.getElementById('generateInvoiceForm');
    form.action = '/clients/recurring-invoices/' + invoiceId + '/generate';
    
    const modal = new bootstrap.Modal(document.getElementById('generateInvoiceModal'));
    modal.show();
}

function pauseInvoice(invoiceId) {
    const form = document.getElementById('pauseInvoiceForm');
    form.action = '/clients/recurring-invoices/' + invoiceId + '/pause';
    
    const modal = new bootstrap.Modal(document.getElementById('pauseInvoiceModal'));
    modal.show();
}

function resumeInvoice(invoiceId) {
    const form = document.getElementById('resumeInvoiceForm');
    form.action = '/clients/recurring-invoices/' + invoiceId + '/resume';
    
    const modal = new bootstrap.Modal(document.getElementById('resumeInvoiceModal'));
    modal.show();
}

function cancelInvoice(invoiceId) {
    const form = document.getElementById('cancelInvoiceForm');
    form.action = '/clients/recurring-invoices/' + invoiceId + '/cancel';
    
    const modal = new bootstrap.Modal(document.getElementById('cancelInvoiceModal'));
    modal.show();
}
</script>
@endpush