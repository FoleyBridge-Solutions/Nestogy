@extends('layouts.app')

@section('title', 'Location Details')

@section('content')
<div class="min-h-screen bg-gray-50">
    <div class="max-w-7xl mx-auto py-6 sm:px-6 lg:px-8">
        <!-- Header -->
        <div class="bg-white shadow rounded-lg mb-6">
            <div class="px-6 py-8 sm:px-6">
                <div class="flex items-center justify-between">
                    <div>
                        <h3 class="text-lg leading-6 font-medium text-gray-900">{{ $location->name }}</h3>
                        <p class="mt-1 max-w-2xl text-sm text-gray-500">Location details for {{ $client->name }}.</p>
                    </div>
                    <div class="flex space-x-3">
                        <a href="{{ route('clients.locations.index', $client) }}" 
                           class="inline-flex items-center px-6 py-2 border border-gray-300 shadow-sm text-sm leading-4 font-medium rounded-md text-gray-700 bg-white hover:bg-gray-50 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500">
                            <svg class="-ml-0.5 mr-2 h-4 w-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 19l-7-7m0 0l7-7m-7 7h18" />
                            </svg>
                            Back to Locations
                        </a>
                        @can('update', $location)
                        <a href="{{ route('clients.locations.edit', [$client, $location]) }}" 
                           class="inline-flex items-center px-6 py-2 border border-transparent text-sm font-medium rounded-md shadow-sm text-white bg-indigo-600 hover:bg-indigo-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500">
                            <svg class="-ml-0.5 mr-2 h-4 w-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M11 5H6a2 2 0 00-2 2v11a2 2 0 002 2h11a2 2 0 002-2v-5m-1.414-9.414a2 2 0 112.828 2.828L11.828 15H9v-2.828l8.586-8.586z" />
                            </svg>
                            Edit Location
                        </a>
                        @endcan
                    </div>
                </div>
            </div>
        </div>

        <!-- Location Details -->
        <div class="bg-white shadow rounded-lg">
            <div class="px-6 py-8 sm:p-6">
                <div class="grid grid-cols-1 gap-y-6 gap-x-4 sm:grid-cols-6">
                    
                    <!-- Location Name -->
                    <div class="sm:flex-1 px-6-span-3">
                        <dt class="text-sm font-medium text-gray-500">Location Name</dt>
                        <dd class="mt-1 text-sm text-gray-900 flex items-center">
                            {{ $location->name }}
                            @if($location->primary)
                                <span class="ml-2 inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-blue-100 text-blue-800">
                                    Primary
                                </span>
                            @endif
                        </dd>
                    </div>

                    <!-- Client -->
                    <div class="sm:flex-1 px-6-span-3">
                        <dt class="text-sm font-medium text-gray-500">Client</dt>
                        <dd class="mt-1 text-sm text-gray-900">
                            <a href="{{ route('clients.show', $client) }}" class="text-indigo-600 hover:text-indigo-900">
                                {{ $client->name }}
                            </a>
                        </dd>
                    </div>

                    <!-- Description -->
                    @if($location->description)
                    <div class="sm:flex-1 px-6-span-6">
                        <dt class="text-sm font-medium text-gray-500">Description</dt>
                        <dd class="mt-1 text-sm text-gray-900">{{ $location->description }}</dd>
                    </div>
                    @endif

                    <!-- Address -->
                    <div class="sm:flex-1 px-6-span-6">
                        <dt class="text-sm font-medium text-gray-500">Address</dt>
                        <dd class="mt-1 text-sm text-gray-900">
                            <div class="space-y-1">
                                @if($location->address)
                                    <div>{{ $location->address }}</div>
                                @endif
                                <div>
                                    {{ $location->city }}, {{ $location->state }} {{ $location->zip }}
                                </div>
                                @if($location->country && $location->country !== 'US')
                                    <div>{{ $location->country }}</div>
                                @endif
                            </div>
                        </dd>
                    </div>

                    <!-- Contact -->
                    <div class="sm:flex-1 px-6-span-3">
                        <dt class="text-sm font-medium text-gray-500">Contact</dt>
                        <dd class="mt-1 text-sm text-gray-900">
                            @if($location->contact)
                                <div>{{ $location->contact->name }}</div>
                                @if($location->contact->title)
                                    <div class="text-gray-500">{{ $location->contact->title }}</div>
                                @endif
                                @if($location->contact->email)
                                    <div class="text-gray-500">
                                        <a href="mailto:{{ $location->contact->email }}" class="text-indigo-600 hover:text-indigo-900">
                                            {{ $location->contact->email }}
                                        </a>
                                    </div>
                                @endif
                            @else
                                <span class="text-gray-500">No contact assigned</span>
                            @endif
                        </dd>
                    </div>

                    <!-- Phone -->
                    <div class="sm:flex-1 px-6-span-3">
                        <dt class="text-sm font-medium text-gray-500">Phone</dt>
                        <dd class="mt-1 text-sm text-gray-900">
                            @if($location->phone)
                                <a href="tel:{{ $location->phone }}" class="text-indigo-600 hover:text-indigo-900">
                                    {{ $location->phone }}
                                </a>
                            @else
                                <span class="text-gray-500">No phone number</span>
                            @endif
                        </dd>
                    </div>

                    <!-- Created/Updated Info -->
                    <div class="sm:flex-1 px-6-span-6 pt-4 border-t border-gray-200">
                        <dl class="grid grid-cols-1 gap-x-4 gap-y-2 sm:grid-cols-2">
                            <div>
                                <dt class="text-sm font-medium text-gray-500">Created</dt>
                                <dd class="text-sm text-gray-900">{{ $location->created_at->format('M j, Y g:i A') }}</dd>
                            </div>
                            <div>
                                <dt class="text-sm font-medium text-gray-500">Last Updated</dt>
                                <dd class="text-sm text-gray-900">{{ $location->updated_at->format('M j, Y g:i A') }}</dd>
                            </div>
                        </dl>
                    </div>

                </div>
            </div>
        </div>

        <!-- Actions -->
        <div class="mt-6 flex justify-end space-x-3">
            @can('update', $location)
            <a href="{{ route('clients.locations.edit', [$client, $location]) }}" 
               class="inline-flex items-center px-6 py-2 border border-transparent text-sm font-medium rounded-md shadow-sm text-white bg-indigo-600 hover:bg-indigo-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500">
                Edit Location
            </a>
            @endcan
            
            @can('delete', $location)
            <form method="POST" 
                  action="{{ route('clients.locations.destroy', [$client, $location]) }}" 
                  class="inline"
                  onsubmit="return confirm('Are you sure you want to delete this location? This action cannot be undone.')">
                @csrf
                @method('DELETE')
                <button type="submit" 
                        class="inline-flex items-center px-6 py-2 border border-transparent text-sm font-medium rounded-md shadow-sm text-white bg-red-600 hover:bg-red-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-red-500">
                    Delete Location
                </flux:button>
            </form>
            @endcan
        </div>
    </div>
</div>
@endsection