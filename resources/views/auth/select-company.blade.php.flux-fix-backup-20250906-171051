@extends('layouts.guest')

@section('title', 'Select Company - Nestogy ERP')

@section('content')
<div class="w-full px-6">
    <div class="flex flex-wrap -mx-4 min-h-screen">
        <!-- Left side - Branding -->
        <div class="w-full lg:w-1/2 hidden lg:flex items-center justify-center bg-blue-600">
            <div class="text-center text-white">
                <div class="mb-6">
                    <img src="{{ asset('static-assets/img/branding/nestogy-logo-white.png') }}" alt="Nestogy" class="max-w-xs w-full h-auto" style="max-width: 200px;">
                </div>
                <h2 class="font-bold mb-6 text-2xl">Welcome Back, {{ $user->name }}</h2>
                <p class="text-lg mb-6">Please select which company you'd like to access</p>
                <div class="grid grid-cols-3 gap-4 text-center">
                    <div>
                        <i class="fas fa-users text-2xl mb-2 block"></i>
                        <p class="text-sm">Client Management</p>
                    </div>
                    <div>
                        <i class="fas fa-ticket-alt text-2xl mb-2 block"></i>
                        <p class="text-sm">Ticket System</p>
                    </div>
                    <div>
                        <i class="fas fa-chart-line text-2xl mb-2 block"></i>
                        <p class="text-sm">Financial Reports</p>
                    </div>
                </div>
            </div>
        </div>

        <!-- Right side - Company Selection -->
        <div class="w-full lg:w-1/2 flex items-center justify-center">
            <div class="w-100" style="max-width: 400px;">
                <div class="bg-white rounded-lg shadow-md overflow-hidden shadow-lg border-0">
                    <div class="p-6 p-8">
                        <!-- Mobile Logo -->
                        <div class="text-center mb-6 block lg:hidden">
                            <img src="{{ asset('static-assets/img/branding/nestogy-logo.png') }}" alt="Nestogy" class="max-w-xs w-full h-auto" style="max-width: 150px;">
                        </div>

                        <h3 class="text-center mb-6 font-bold text-xl">Select Company</h3>
                        <p class="text-center mb-6 text-gray-600">You have access to multiple companies. Please select which one you'd like to access:</p>

                        @if ($errors->any())
                            <div class="px-6 py-6 rounded bg-red-100 border border-red-400 text-red-700 mb-6">
                                <ul class="mb-0">
                                    @foreach ($errors->all() as $error)
                                        <li>{{ $error }}</li>
                                    @endforeach
                                </ul>
                            </div>
                        @endif

                        <form method="POST" action="{{ route('auth.company-select') }}">
                            @csrf

                            <!-- Company Selection -->
                            <div class="mb-6">
                                @foreach($companies as $company)
                                    <div class="mb-6">
                                        <label class="flex items-center p-6 border border-gray-200 rounded-lg hover:bg-gray-50 cursor-pointer transition-colors">
                                            <input type="radio" name="company_id" value="{{ $company->id }}" 
                                                   class="w-4 h-4 text-blue-600 border-gray-300 focus:ring-blue-500"
                                                   {{ $loop->first ? 'checked' : '' }}>
                                            <div class="ml-3 flex-1">
                                                <div class="flex items-center">
                                                    <i class="fas fa-building text-blue-600 mr-2"></i>
                                                    <span class="font-medium text-gray-900">{{ $company->name }}</span>
                                                </div>
                                                @if($company->description)
                                                    <p class="text-sm text-gray-500 mt-1">{{ $company->description }}</p>
                                                @endif
                                            </div>
                                        </flux:label>
                                    </div>
                                @endforeach
                            </div>

                            <!-- Submit Button -->
                            <div class="mb-6">
                                <button type="submit" class="w-full inline-flex justify-center items-center px-6 py-2 bg-blue-600 text-white font-medium rounded-md hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-blue-500 text-lg py-6">
                                    <i class="fas fa-arrow-right mr-1"></i>Continue
                                </flux:button>
                            </div>

                            <!-- Logout Link -->
                            <div class="text-center">
                                <a href="{{ route('logout') }}" onclick="event.preventDefault(); document.getElementById('logout-form').submit();" class="text-blue-600 hover:text-blue-700">
                                    <i class="fas fa-sign-out-alt mr-1"></i>Sign out and use different account
                                </a>
                            </div>

                            <form id="logout-form" action="{{ route('logout') }}" method="POST" class="hidden">
                                @csrf
                            </form>
                        </form>
                    </div>
                </div>

                <!-- Footer -->
                <div class="text-center mt-6">
                    <p class="text-gray-600 text-sm">
                        &copy; {{ date('Y') }} Nestogy ERP. All rights reserved.
                    </p>
                </div>
            </div>
        </div>
    </div>
</div>

@push('styles')
<style>
/* Custom gradient background */
.bg-primary-gradient {
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
}

/* Enhanced input styling */
input[type="radio"]:focus {
    --tw-ring-color: rgb(59 130 246 / 0.3);
    border-color: #667eea;
}

/* Enhanced button styling */
button[type="submit"] {
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    transition: all 0.3s ease;
}

button[type="submit"]:hover {
    transform: translateY(-1px);
    box-shadow: 0 5px 15px rgba(102, 126, 234, 0.4);
}

/* Company selection styling */
input[type="radio"]:checked + div {
    color: #1f2937;
}

input[type="radio"]:checked {
    background-color: #667eea;
    border-color: #667eea;
}

@media (max-width: 991.98px) {
    .lg\\:w-1\\/2 {
        padding: 2rem;
    }
}
</style>
@endpush
@endsection